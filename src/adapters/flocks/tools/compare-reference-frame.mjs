#!/usr/bin/env node
// SPDX-License-Identifier: GPL-2.0-or-later

// A selected-still RGB score would overclaim parity across the intentional
// lighting deviation. Run the qualified state, projection, continuity, and
// diagnostic RGB sequence comparison instead.
await import("./compare-frame-sequence.mjs");
