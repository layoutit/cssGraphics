import { selectReallySlickPaletteVariant } from "../../../shared/reallyslickPalette.mjs";

export function selectCycloneStartupPaletteVariant(paletteVariantIds, options = {}) {
  return selectReallySlickPaletteVariant(paletteVariantIds, options);
}
