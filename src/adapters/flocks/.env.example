# Optional alternate generated-public root for isolated preparation or testing.
CSSFLOCKS_GENERATED_PUBLIC_DIR=

# Set to 1 only for the local deploy build that writes dist/site/flocks and
# copies the generated cssflocks tree into dist/site/cssflocks.
CSSFLOCKS_DEPLOY_BUILD=0
